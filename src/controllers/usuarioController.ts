import WebSocket from "ws";
import { IMessageSocket } from "..";

export class UsuarioController {
    constructor() {

    }
    public async create<T>(message: IMessageSocket<T>): Promise<void> {
        
    }
    public async store(): Promise<void> {

    }
}